export class CharacterDecomposition {
  Parts: string;
  Characters: string;

  constructor(parts?: string, characters?: string) {
    this.Parts = parts || '';
    this.Characters = characters || '测试';
  }
}
